Write a program to print the product of even numbers from 1 to n.

#include <stdio.h>

int main() {
    int n, i;
    unsigned long long product = 1; // use long long to handle large products

    printf("Enter the value of n: ");
    scanf("%d", &n);

    if (n < 2) {
        printf("No even numbers in the range 1 to %d.", n);
    } else {
        printf("Even numbers from 1 to %d are: ", n);
        for (i = 2; i <= n; i += 2) {
            printf("%d ", i);
            product *= i;
        }
        printf("\nProduct of even numbers = %llu", product);
    }

    return 0;
}
